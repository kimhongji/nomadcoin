curl http://localhost:8000

curl -X POST -H 'Content-Type: application/json' 'http://localhost:8000/blocks' -d '{"message" : "what.."}'


curl localhost:8000/blocks | jq


curl  http://localhost:4000/status

curl  http://localhost:4000/blocks

go run main.go run -mode=rest

http://localhost:4000/ // web 으로 접속하면 사용 가능한 api list 보임


curl -X POST -H 'Content-Type: application/json' 'http://localhost:4000/transactions' -d '{"to" : "satoshi", "amount": 70 }'
